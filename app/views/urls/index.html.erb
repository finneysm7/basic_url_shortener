<h1>Shortened urls</h1>
<ul>
	<% @urls.each do |url| %>
	<li>New Url: <%= link_to url.name, url.link%><br>
		Original Url: <%= link_to url.link, url.link%><br>
		<%= link_to "Statistics for this site", url_stats_url(url)%>
	</li>
	<%end%>
</ul>

<%= link_to "New Url", new_url_url%>